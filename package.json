{
  "name": "app-deliciadoces",
  "version": "0.1.0",
  "private": true,
  "description": "Sistema de gestão empresarial para a Delícia Doces — controle de fluxo de caixa, estoque e contas a pagar/receber, com dashboard financeiro.",
  "license": "UNLICENSED",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "npm run dev --workspace=backend & npm run dev --workspace=frontend",
    "dev:backend": "npm run dev --workspace=backend",
    "dev:frontend": "npm run dev --workspace=frontend",
    "build": "npm run build --workspace=frontend",
    "prisma:generate": "npm run prisma:generate --workspace=backend",
    "prisma:migrate": "npm run prisma:migrate --workspace=backend",
    "prisma:studio": "npm run prisma:studio --workspace=backend",
    "db:seed": "npm run db:seed --workspace=backend",
    "vercel-build": "npm run prisma:generate --workspace=backend && npm run build --workspace=frontend",
    "db:seed:demo": "npm run db:seed:demo --workspace=backend"
  }
}
